# Test Data

Use these fixtures for local and automated tests.

- `sample-signal-ingest.json` is a representative raw payload for the open ingest route.
- It is intentionally small so it can be copied into unit tests or replay scripts.

If you need hardware-backed parity, capture the upstream payload format from the RC2 runtime and compare it against this fixture shape.
